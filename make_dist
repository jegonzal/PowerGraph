#!/bin/bash


version=`hg summary | grep parent | sed 's/parent: //g' | sed 's/:.*//g'`
echo "Version: $version"
tar --exclude=debug --exclude=release --exclude=profile --exclude=.hg \
    --exclude=dist \
    -C ../ \
    -vz \
    -cf dist/graphlab_v${version}.tar.gz \
    graphlabapi




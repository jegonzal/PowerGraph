function set_vertex_data(handle, vertex, vdata) %#eml
% EXTERN_VERTEX_STRUCT sets the type of local variable vdata
%#EXTERN_VERTEX_STRUCT#%

eml.ceval('emx_set_vertex_data', handle, vertex, eml.ref(vdata));
end
